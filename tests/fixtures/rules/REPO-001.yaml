rule: REPO-001
negative:
  files:
    .gitignore: |
      node_modules/
      dist/
positive:
  # An empty .gitignore ignores nothing: presence without content is MISSING.
  files:
    .gitignore: ''
  expect: MISSING
