rule: SUP-001
negative:
  files:
    package-lock.json: |
      { "lockfileVersion": 3 }
    requirements-dev.txt: |
      # Fully pinned development toolchain.
      requests==2.31.0
positive:
  files:
    package.json: |
      { "name": "app" }
  expect: MISSING
