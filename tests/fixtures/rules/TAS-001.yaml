rule: TAS-001
negative:
  files:
    tests/unit/pricing.test.ts: |
      import { describe, it, expect } from 'vitest';
      describe('pricing', () => {
        it('adds tax', () => {
          expect(1 + 1).toBe(2);
        });
      });
positive:
  files:
    README.md: |
      # Project
    src/index.ts: |
      export const x = 1;
  expect: MISSING
