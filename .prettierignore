dist/
coverage/
node_modules/
package-lock.json

# A deliberately unformatted, deliberately defective fixture. Formatting it
# would move the line numbers that examples/sample-report.md cites.
examples/demo-app/

# Generated artefact.
AUDIT.md

# Experiment evidence (docs/EXPERIMENT_PROTOCOL.md). These reports are
# machine-generated audit output preserved verbatim; formatting them would
# alter the very line numbers and tables the run is evidence for.
experiments/
examples/sample-report.md
.gitleaks.toml

# Machine-written by release-please (double blank lines after headers).
# Formatting it by hand fights the generator on every Release PR.
CHANGELOG.md

# KEY=VALUE, not a prettier language. Must stay byte-stable: docs quote it.
.env.example
