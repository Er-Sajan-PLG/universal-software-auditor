---
'@xenos1996/usa': patch
---

Migrate the release pipeline to a pnpm workspace with changesets: declared bumps replace history-guessing, `packages/*` is pre-wired for the next package, and the publish/provenance legs move to a tag-following `publish.yml`.
